-- 操作题 q26–q30 已按 .ipynb 重建 code_segments，blank 索引/数量/总分均变更。
-- 旧 op_sessions 中的 blanks_draft 是按旧 blank 索引存的，无法再正确还原；
-- 全清这 5 题的 session 记录（含已提交），让用户重新作答。
DELETE FROM op_sessions WHERE operation_id IN (26, 27, 28, 29, 30);
