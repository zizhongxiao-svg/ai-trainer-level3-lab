[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v --tb=short -m "not kernel"
markers =
    kernel: tests that actually spawn a jupyter kernel subprocess (slow, opt-in via -m kernel)
asyncio_mode = auto
